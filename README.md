---
title: "regextoy"
output: github_document
---

# regextoy

`regextoy` is a **toy R package** to practice creating R packages.  
It currently contains **one function**:

## Function: strsplit1

`strsplit1()` splits a string into pieces using a delimiter.

### Usage

```r
strsplit1(x, split)
